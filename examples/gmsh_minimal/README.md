# gmsh_minimal

A tiny CaeReflex-created Gmsh-style `.geo` parser example. It demonstrates core-mode `.geo` inspection and does not require Gmsh or meshio.

Run:

```bash
caereflex inspect-gmsh examples/gmsh_minimal/t1.geo --out build/gmsh_case.json
```

This is not a mesh-quality or design-safety example.
