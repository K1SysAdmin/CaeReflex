# CROSSREF

CaeReflex uses CrossRef for DOI metadata and available abstracts only. It does not retrieve full papers, scrape publishers, fabricate abstracts, or validate simulations. CrossRef calls are explicit-only and may use mailto/User-Agent, caching, and mocked responses for tests.
