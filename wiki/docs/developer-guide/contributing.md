# Contributing

Keep business logic in `caereflex.services`, domain records in `caereflex.core.models`, and file-format-specific extraction inside adapters.

Documentation updates should keep the root docs and wiki consistent with package facts. Release-affecting changes must update the release controls checklist.
